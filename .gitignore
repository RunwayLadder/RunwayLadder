node_modules/
dist/
.turbo/
*.tsbuildinfo

# Anchor and Cargo output. CARGO_TARGET_DIR lives inside WSL, but a stray local
# build must not reach the index either.
target/
.anchor/
test-ledger/

.env
.env.*
!.env.example

*.json.keypair
*-keypair.json
id.json

*.md
!README.md

# End-to-end stand: keys, the ledger log and the setup description. Disposable
# by construction — every `pnpm e2e:stand` wipes it and brings it up again.
.e2e/
